package com.algorithm;

// 冒泡排序基本思想 : 通过对待排序的序列从前向后(下标较小的元素开始),依次比较相邻元素的值，若发现逆序则交换，
// 使元素较大的值从前向后移动
public class BubbleSort {

    public static void main(String[] args) {

    }

    public static void sort(int[] arr){
        int min = 0;
        int temp = 0;
        for(int i = 0; i < arr.length; i++){
        }
    }
}
